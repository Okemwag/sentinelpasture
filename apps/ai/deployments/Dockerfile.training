FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /srv

COPY apps/ai apps/ai

RUN pip install --upgrade pip && pip install -e apps/ai

CMD ["python", "-m", "ai.training.retrain_pipeline"]
