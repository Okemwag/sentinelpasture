# Infrastructure

This directory defines the deployment and operational shape of the platform.

- `terraform/` for cloud provisioning.
- `k8s/` for raw Kubernetes manifests and overlays.
- `helm/` for service packaging.
- `scripts/` for bootstrap, migration, and seeding helpers.
