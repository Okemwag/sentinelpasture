# Model Governance

Every model output should carry:

- `model_version`
- `feature_snapshot_timestamp`
- `confidence_band`
- `top_drivers`
- `known_data_gaps`

The platform should prefer calibrated, governable models over opaque complexity.
Intervention recommendations must be policy-constrained and reviewable.
